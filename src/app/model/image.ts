export interface Image {
  id: number | null,
  created: Date | null,
  updated: Date | null,
  fileName: string | null,
  fileSize: string | null,
  path: string | null,
  yardId: number | null,
  localFile: string | null
}
